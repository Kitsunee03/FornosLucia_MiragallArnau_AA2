#ifndef ENUMS_H
#define ENUMS_H

enum class CELL { INVALID, EMPTY, WALL, POKEMON, POKEBALL, PLAYER };
enum class SCENE { MAIN_MENU, MAP, COMBAT, END_MENU };
enum class ACTIONS { FIGHT, CAPTURE, RUN };

#endif